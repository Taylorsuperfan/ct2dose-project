# Checkpoints

Large model checkpoints are not included by default.

Current best profile-refinement checkpoint:
- ct2dose_phase10d_falloff_aware_direction_refinement_seed42_best.pt

Store large checkpoints externally or use Git LFS if they must be versioned.
